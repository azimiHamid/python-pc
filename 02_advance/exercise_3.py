# import utils
numbers = [734, 239, 234, 564, 690, 455, 789]
# print(utils.find_max())


from utils import find_max
# max_number = find_max(numbers)
# print(max_number)

# we ca also use max() method to find the maximum number in a list
print(max(numbers))
